<h1>Editing Bank Account</h1>

<%= render 'form', bank_account: @bank_account %>

<%= link_to 'Show', polymorphic_path([@accountable, @bank_account]) %> |
<%= link_to 'Back', polymorphic_path([@accountable, BankAccount]) %>
